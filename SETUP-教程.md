# CS2 校园杯 · 上线教程(腾讯云 CloudBase)

这份教程带你把网页接到腾讯云「云开发 CloudBase」,让**报名数据和赛事信息全网共享**——所有人看到同一份名单,你在后台改一次,访客刷新就能看到。

> 全程只有你能做的部分:注册腾讯云 + 实名认证(需要你的身份证/微信)。其余都是复制粘贴。
> 预计 20~30 分钟。**没配置之前,网页也能正常打开**,只是处于「演示模式」(报名只存在本地浏览器)。

---

## 你手上的文件

| 文件 | 作用 | 要不要改 |
|---|---|---|
| `index.html` | 公开主页:报名、战队名单、实时赛程对阵表 | ❌ 不用改 |
| `past.html` | 公开页:往届赛事相册(照片墙) | ❌ 不用改 |
| `admin.html` | 后台管理:登录后改赛事信息、审核报名、录入赛果、上传往届照片 | ❌ 不用改 |
| `lc.js` | 数据层(连接云端的代码) | ❌ 不用改 |
| `config.js` | **配置文件——只改这一个** | ✅ 填 3 个值 |
| `数据库初始化.sql` | 一段建表+设权限的 SQL,第 4 步用 | ❌ 直接用 |
| `SETUP-教程.md` | 就是本文件 | — |

五个网页文件(`index.html`、`past.html`、`admin.html`、`lc.js`、`config.js`)必须**放在同一个文件夹**里一起上传。`数据库初始化.sql` 只在控制台里粘一次,不用上传。

---

## 第 1 步:注册腾讯云并实名认证

1. 打开 https://cloud.tencent.com ,用微信/QQ 注册登录。
2. 完成**实名认证**(个人认证即可,扫脸几分钟通过)。
   - 没实名不能创建云开发环境,这步跳不过。
   - 学生用个人认证就行,不需要企业认证。

## 第 2 步:开通云开发,创建环境

1. 打开云开发控制台:https://tcb.cloud.tencent.com/dev
2. 点「新建环境」。
   - **计费方式**:选「按量计费」。校园比赛这点访问量,基本在免费额度内,几乎不产生费用(可留意控制台的用量提示)。
   - **地域**:选离你近的,比如「上海」。**记住你选的地域**,第 3 步要用。
   - 环境名称随便起,例如 `cs2cup`。
3. 创建完成后进入这个环境。

> 地域对应的代号:上海=`ap-shanghai`,广州=`ap-guangzhou`,北京=`ap-beijing`,新加坡=`ap-singapore`。

## 第 3 步:拿到 3 个值,填进 config.js

### ① 环境 ID(env)
控制台环境首页,标题下方那串,形如 `cs2cup-1a2b3c4d`。复制它。

### ② Publishable Key(accessKey)
1. 打开 API Key 配置页:https://tcb.cloud.tencent.com/dev#/env/apikey
2. 找到 **Publishable Key**(客户端公钥),点「新建」生成一个,复制。

> ⚠️ **安全红线,务必看清:**
> - 这里要填的是环境的 **Publishable Key(客户端公钥)**。官方明确说明它**可以安全地暴露在前端网页里**,别人拿到也只能在你设定的「安全规则」范围内操作,没有风险。
> - **绝对不要**把腾讯云账号的 **SecretId / SecretKey**(在「访问管理」里那种)填到 `config.js`!那是**账号级私钥**,泄露 = 整个云账号被人控制。本项目任何地方都不需要它。
> - 判断方法:如果某个 Key 的说明里有「不可泄露/服务器端使用」,那就是私钥,**不要**用在这里。

### ③ 地域(region)
就是第 2 步你选的地域代号,例如 `ap-shanghai`。

### 填进去
用记事本/VS Code 打开 `config.js`,把三个值填进引号内:

```js
window.LC_CONFIG = {
  env:       "cs2cup-1a2b3c4d",              // ← 换成你的 环境ID
  accessKey: "你的-Publishable-Key",         // ← 换成你的 Publishable Key
  region:    "ap-shanghai"                   // ← 换成你的地域
};
```

保存。

## 第 4 步:建表 + 设权限(粘一段 SQL,一步到位)

你的环境是 **PostgreSQL 版**云开发。不用手动点「新建表」——我准备了一段 SQL,它会自动建好业务表并配好安全权限。

1. 控制台左侧 → **数据库** → 进入 **PostgreSQL**。
2. 找到 **「SQL 编辑器」**(你截图里那个页面就有)。
3. 用记事本打开 `数据库初始化.sql`,**全选、复制**,粘进 SQL 编辑器。
4. 点 **「运行」**。看到成功提示即可(脚本可重复运行,多跑几次也不会坏)。

它一次性做好了:
- 建五张表:`event`(赛事信息)、`team`(完整报名)、`team_public`(公开战队资料)、`gallery`(往届赛事相册)、`cs2cup_admin`(管理员白名单);
- 打开行级安全(RLS);
- 设好权限:**访客只能看公开名单/往届照片，并通过受控入口提交报名；只有管理员白名单中的账号能改赛事、删战队、传/删往届照片。**

> 跑完回到「表管理」能看到上述五张表,就成功了。旧版数据会自动迁移出公开副本；**仍需完成第 6 步，把你的管理员 UID 加入白名单。**

## 第 5 步:开启邮箱登录

在控制台进入 **身份认证 / 认证**(左侧菜单),开启 **邮箱登录(邮箱 + 密码)**——给你后台登录用。

> 访客报名**不需要登录**:网页用你的 Publishable Key 以「访客(anon)」身份就能读名单、交报名,这已经由第 4 步的权限管好了。所以你**只需开邮箱登录**这一项。

## 第 6 步:创建并授权管理员账号

> ⚠️ 后台网页不提供公开注册。**登录成功不等于管理员**；只有数据库白名单中的 UID 才能写数据。

1. 在 CloudBase 控制台的「身份认证 / 用户管理」中创建或确认你的邮箱账号；开启「邮箱登录(邮箱 + 密码)」。
2. 在用户管理中复制该账号的 **UID**。
3. 打开 PostgreSQL 的「SQL 编辑器」，执行（替换单引号中的内容）：

   ```sql
   insert into public.cs2cup_admin (user_id)
   values ('你的管理员 UID')
   on conflict do nothing;
   ```

4. 把网站部署到 `https://` 地址后，用该邮箱登录 `admin.html`。显示绿色“已获后台管理员授权”即完成。

> 如需新增或撤销管理员，分别在 SQL 编辑器执行 `insert` 或 `delete from public.cs2cup_admin where user_id = 'UID';`。不要把 UID 或数据库私钥写进 `config.js`。

## 第 7 步:把网页发布上线

最省事的方式是用 CloudBase 自带的**静态网站托管**:

1. 控制台左侧 → **静态网站托管**,首次使用点开通(会用到一点对象存储,免费额度足够)。
2. 把这 5 个文件上传到根目录:`index.html`、`past.html`、`admin.html`、`config.js`、`lc.js`。
3. 开通后会给你一个默认访问域名(形如 `https://你的环境.tcloudbaseapp.com`)。
   - 公开报名页:`https://你的环境.tcloudbaseapp.com/index.html`
   - 往届赛事页:`https://你的环境.tcloudbaseapp.com/past.html`(主页导航「往届赛事」也能进)
   - 后台管理页:`https://你的环境.tcloudbaseapp.com/admin.html`
4. 把 `index.html` 的链接发到群里/公众号,就可以收报名了。

> 也可以放到任何静态服务器(GitHub Pages、Vercel、你自己的服务器)。用**自己的域名**且服务器在中国大陆,需要 **ICP 备案**;用上面的默认域名或海外托管则不用你操心备案。

---

## 日常怎么用

- **改比赛名 / 报名截止时间 / 赛程开始时间 / 联系方式 / 规则 / 地图**:登录 `admin.html` → 「赛事信息」标签 → 改完保存,公开页刷新即生效。
  - 「赛程开始时间」决定主页倒计时和对阵表的时间;「报名截止」会显示在须知里。
  - 规则每行一条,用 `标题|说明` 格式,例如:`赛制|单败淘汰,每轮 BO3`。
  - 地图池用逗号分隔,例如:`Mirage,Inferno,Nuke,Ancient,Anubis,Overpass,Dust II`。
- **审核报名 / 导出名单**:`admin.html` → 「报名管理」标签,可搜索；新报名默认「待审核」，切换为「已通过」后才会公开显示。可拒绝无效报名、删除测试数据，或**导出 CSV**(用 Excel 打开,中文正常)。
- **录入淘汰赛赛果**:`admin.html` → 「赛程与赛果」标签。这里只列出已通过战队；前 3 轮填写 BO3 比分（胜方 2 分），总决赛填写 BO5 比分（胜方 3 分）。保存后首页自动展示胜者和下一轮对阵。若更改某场结果，受影响的后续比分会清空，待新对阵确定后重新填写。
- **传往届照片**:`admin.html` → 「往届相册」标签 → 填「届次」(如 `2025 秋季赛`,同一届会归到一组)和「照片说明」→ 点「选择照片上传」可一次选多张,上传后立刻出现在公开页 `past.html`。照片会自动压缩,建议整体几十张以内;删除在同一页每张下方点「删除」。
- **删测试数据**:在「报名管理」里逐条删除即可。

---

## 隐私与安全说明(请读一遍)

- `team` 只保存管理员可读的完整报名资料；公开页读取的是自动同步的 `team_public`，其中不含队长、联系方式和备注。
- 后台安全**不靠隐藏 `admin.html`**：即使知道管理员邮箱或能注册普通账号，也无法写入。RLS 仅允许 `cs2cup_admin` 白名单中的 UID 管理赛事、报名和相册。
- 报名名额、截止时间、名称/TAG 去重和种子分配由数据库函数在一个事务中处理；浏览器校验只负责更快地提示。
- 再次强调:`config.js` 里只放 **Publishable Key(公钥)**,绝不放账号 SecretId/SecretKey。

---

## 常见问题排错

**点「发送验证码 / 登录」报 `Failed to fetch`(发送失败)?**
→ 你是**双击打开的 `admin.html`**(地址栏是 `file:///...`)。浏览器禁止 `file://` 页面连云端。
→ 解决:先做**第 7 步**把网页传到静态托管,然后用 `https://你的环境.tcloudbaseapp.com/admin.html` 这个**网址**打开,再创建账号/登录即可。(别再双击本地文件了。)

**后台一直显示黄色「未连接云端 / 未配置」?**
→ `config.js` 的 `env` 或 `accessKey` 没填对,或还留着「在此粘贴…」。检查后**刷新页面**。

**报名 / 保存时提示「无权限 / permission denied」?**
→ 几乎都是第 4 步的 `数据库初始化.sql` 还没跑,或没跑成功。回到「SQL 编辑器」把它重新粘一遍、点运行(脚本可重复运行)。跑完再回网页刷新重试。
→ 保存赛事 / 删战队时报无权限,还要确认你**已获后台管理员授权**(顶部有绿色提示)，并且 UID 已加入 `cs2cup_admin` 白名单。

**公开页打开是「演示模式」,报名没进云端?**
→ 未配置云端时，演示模式下的报名只保存在本地浏览器。若已配置云端却显示「云端暂不可用」，网站会暂停报名，避免误报提交成功；检查 config、网络和第 4 步 SQL 是否成功执行。

**倒计时时间不对 / 赛程时间想改?**
→ 后台「赛事信息」里设「赛程开始时间」。不填的话,程序默认取「下一个周六中午 12:00」。

**登录成功后提示“未获后台授权”?**
→ 这是正常的安全拦截：在「身份认证 / 用户管理」复制该账号 UID，并按第 6 步将它插入 `public.cs2cup_admin`，随后退出并重新登录。

---

有任何一步卡住,把控制台的报错截图发我,我帮你看。祝比赛顺利 🎯
